services:
  letta:
    image: letta/letta:latest
    container_name: nameless-letta
    ports:
      - "8283:8283"
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
    volumes:
      - letta_data:/root/.letta
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8283/v1/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

  # Optional: PostgreSQL for production deployments
  # Uncomment if you want external database instead of Letta's embedded SQLite
  # postgres:
  #   image: postgres:16-alpine
  #   container_name: nameless-postgres
  #   environment:
  #     POSTGRES_USER: letta
  #     POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-lettapass}
  #     POSTGRES_DB: letta
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   ports:
  #     - "5432:5432"
  #   restart: unless-stopped

  # Optional: Redis for caching
  # redis:
  #   image: redis:7-alpine
  #   container_name: nameless-redis
  #   ports:
  #     - "6379:6379"
  #   volumes:
  #     - redis_data:/data
  #   restart: unless-stopped

volumes:
  letta_data:
  # postgres_data:
  # redis_data:
